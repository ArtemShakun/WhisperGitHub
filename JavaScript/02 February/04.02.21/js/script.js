// TASK-1 -- Создайте функцию, которая входящий массив преобразует в объект

// let transformToObject = arr => {
//    let newObj = {};

//    for (let el of arr) {
//       newObj[el] = el;
//    }
//    return newObj
// }

// TASK-2 -- Создайте функцию, которая складывает все значения свойств объектов в массив

// let concatProps = obj => {
//    const arr = [];

//    for (let key in obj) {
//       arr.push(obj[key])
//    }

//    return arr
// }

// TASK-3--Создайте функцию, которая найдет в объекте всех людей, которым исполнилось 18 лет

// let getAdults = userObj => Object.entries(userObj)
//    .filter(elem => (elem[1] >= 18))
//    .map(name => name[0]);

// TASK-4--Создайте функцию, которая будет копировать объект

// let copyObj = obj => Object.assign(obj);

